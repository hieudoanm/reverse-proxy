{
  "name": "reverse-proxy",
  "version": "0.0.1",
  "description": "Reverse Proxy",
  "main": "main.go",
  "scripts": {
    "start": "./bin/main",
    "prebuild": "rimraf public && docsify init public",
    "build": "go build -o ./bin/main",
    "dev": "go run ./api/main.go",
    "format": "prettier --cache --write . && gofmt -s -w .",
    "prepare": "husky || true"
  },
  "keywords": [
    "golang"
  ],
  "author": "hieudoanm <hieumdoan@gmail.com> (https://hieudoanm.github.io)",
  "license": "GPL-3.0",
  "devDependencies": {
    "docsify-cli": "4.4.4",
    "husky": "9.1.7",
    "prettier": "3.4.2",
    "rimraf": "6.0.1"
  }
}
